# Normalize line endings to LF in the repo; check out native on each platform.
* text=auto eol=lf

# Treat these as binary (no EOL conversion / diff).
*.pdf binary
*.png binary
*.jpg binary
*.ico binary
